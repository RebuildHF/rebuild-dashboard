<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
<title>Rebuild Coach Development</title>
<style>
  :root {
    --blue: #3478EB;
    --blue-dark: #1a5bc4;
    --blue-light: #E8F0FD;
    --dark: #111827;
    --black: #000;
    --white: #fff;
    --offwhite: #F4F6FB;
    --text: #1A1A1A;
    --muted: #6B7280;
    --border: #E5E7EB;
    --green: #10B981;
    --orange: #F59E0B;
    --red: #EF4444;
    --locked: #9CA3AF;
    --radius: 10px;
    padding-top: env(safe-area-inset-top, 0px);
    padding-bottom: env(safe-area-inset-bottom, 0px);
    box-sizing: border-box;
  }
  *, *::before, *::after { box-sizing: inherit; margin: 0; padding: 0; }
  body { font-family: Calibri, 'Segoe UI', sans-serif; background: var(--offwhite); color: var(--text); min-height: 100vh; }

  /* ── Auth ── */
  #auth-screen { display:flex; align-items:center; justify-content:center; min-height:100vh; padding:20px; }
  .auth-card { background:var(--white); border-radius:16px; padding:40px; width:100%; max-width:420px; box-shadow:0 4px 24px rgba(0,0,0,.10); }
  .auth-logo { display:flex; align-items:center; gap:12px; margin-bottom:32px; }
  .auth-logo-mark { width:44px; height:44px; background:var(--blue); border-radius:10px; display:flex; align-items:center; justify-content:center; }
  .auth-logo-mark span { color:var(--white); font-weight:800; font-size:18px; }
  .auth-logo-text h1 { font-family:Cambria,serif; font-size:20px; color:var(--dark); }
  .auth-logo-text p { font-size:12px; color:var(--muted); }
  .auth-card h2 { font-family:Cambria,serif; font-size:24px; margin-bottom:8px; }
  .auth-card p { color:var(--muted); font-size:14px; margin-bottom:28px; }
  .field { margin-bottom:16px; }
  .field label { display:block; font-size:13px; font-weight:600; color:var(--dark); margin-bottom:6px; }
  .field input { width:100%; padding:10px 14px; border:1.5px solid var(--border); border-radius:8px; font-size:15px; outline:none; transition:border .2s; }
  .field input:focus { border-color:var(--blue); }
  .btn { display:inline-flex; align-items:center; justify-content:center; gap:8px; padding:11px 20px; border-radius:8px; font-size:14px; font-weight:600; cursor:pointer; border:none; transition:all .15s; }
  .btn-primary { background:var(--blue); color:var(--white); width:100%; }
  .btn-primary:hover { background:var(--blue-dark); }
  .btn-sm { padding:7px 14px; font-size:13px; }
  .btn-ghost { background:transparent; color:var(--blue); border:1.5px solid var(--blue); }
  .btn-ghost:hover { background:var(--blue-light); }
  .btn-danger { background:#FEF2F2; color:var(--red); border:1.5px solid #FECACA; }
  .btn-success { background:#ECFDF5; color:var(--green); border:1.5px solid #A7F3D0; }
  .error-msg { color:var(--red); font-size:13px; margin-top:8px; display:none; }

  /* ── App shell ── */
  #app { display:none; }
  .topbar { background:var(--dark); height:58px; display:flex; align-items:center; padding:0 20px; gap:16px; position:sticky; top:0; z-index:100; }
  .topbar-logo { display:flex; align-items:center; gap:10px; }
  .topbar-mark { width:32px; height:32px; background:var(--blue); border-radius:7px; display:flex; align-items:center; justify-content:center; }
  .topbar-mark span { color:var(--white); font-weight:800; font-size:13px; }
  .topbar-name { color:var(--white); font-family:Cambria,serif; font-size:16px; }
  .topbar-right { margin-left:auto; display:flex; align-items:center; gap:10px; }
  .role-badge { padding:3px 10px; border-radius:20px; font-size:11px; font-weight:700; letter-spacing:.5px; text-transform:uppercase; }
  .role-admin { background:#7C3AED; color:var(--white); }
  .role-mentor { background:var(--blue); color:var(--white); }
  .role-coach { background:var(--green); color:var(--white); }
  .topbar-user { color:#9CA3AF; font-size:13px; }
  .logout-btn { background:transparent; border:1px solid #374151; color:#9CA3AF; font-size:12px; padding:5px 10px; border-radius:6px; cursor:pointer; }
  .logout-btn:hover { border-color:#6B7280; color:var(--white); }

  /* ── Nav tabs ── */
  .nav-tabs { background:var(--white); border-bottom:1px solid var(--border); padding:0 20px; display:flex; gap:0; overflow-x:auto; }
  .nav-tab { padding:14px 18px; font-size:14px; font-weight:600; color:var(--muted); cursor:pointer; border-bottom:2.5px solid transparent; white-space:nowrap; transition:all .15s; }
  .nav-tab:hover { color:var(--blue); }
  .nav-tab.active { color:var(--blue); border-bottom-color:var(--blue); }

  /* ── Main content ── */
  .main { padding:24px 20px; max-width:1100px; margin:0 auto; }

  /* ── Cards ── */
  .card { background:var(--white); border-radius:var(--radius); padding:20px; border:1px solid var(--border); margin-bottom:16px; }
  .card-header { display:flex; align-items:center; justify-content:space-between; margin-bottom:16px; }
  .card-title { font-family:Cambria,serif; font-size:18px; color:var(--dark); }
  .card-sub { font-size:13px; color:var(--muted); margin-top:2px; }

  /* ── Phase grid ── */
  .phase-grid { display:grid; grid-template-columns:repeat(auto-fill,minmax(300px,1fr)); gap:16px; }
  .phase-card { background:var(--white); border-radius:var(--radius); border:1px solid var(--border); overflow:hidden; transition:box-shadow .15s; }
  .phase-card:hover { box-shadow:0 4px 16px rgba(0,0,0,.08); }
  .phase-header { padding:14px 16px; display:flex; align-items:center; gap:12px; }
  .phase-badge { width:40px; height:40px; border-radius:8px; display:flex; align-items:center; justify-content:center; font-weight:800; font-size:15px; flex-shrink:0; }
  .phase-unlocked .phase-badge { background:var(--blue); color:var(--white); }
  .phase-locked .phase-badge { background:#F3F4F6; color:var(--locked); }
  .phase-title { font-family:Cambria,serif; font-size:15px; color:var(--dark); }
  .phase-locked .phase-title { color:var(--muted); }
  .phase-weeks { font-size:12px; color:var(--muted); }
  .phase-body { padding:0 16px 16px; }
  .sub-list { list-style:none; }
  .sub-item { display:flex; align-items:center; gap:10px; padding:8px 0; border-bottom:1px solid var(--border); cursor:pointer; transition:background .1s; border-radius:6px; padding:8px; margin:0 -8px; }
  .sub-item:last-child { border-bottom:none; }
  .sub-item:hover { background:var(--blue-light); }
  .sub-icon { width:28px; height:28px; border-radius:6px; display:flex; align-items:center; justify-content:center; font-size:13px; flex-shrink:0; }
  .sub-unlocked .sub-icon { background:var(--blue-light); }
  .sub-locked .sub-icon { background:#F3F4F6; }
  .sub-name { font-size:13px; font-weight:600; flex:1; }
  .sub-unlocked .sub-name { color:var(--dark); }
  .sub-locked .sub-name { color:var(--locked); }
  .sub-status { font-size:11px; }
  .lock-date { color:var(--muted); font-size:11px; margin-top:8px; text-align:center; padding:8px; background:var(--offwhite); border-radius:6px; }

  /* ── Session modal ── */
  .modal-overlay { position:fixed; inset:0; background:rgba(0,0,0,.5); z-index:200; display:none; align-items:center; justify-content:center; padding:16px; }
  .modal-overlay.open { display:flex; }
  .modal { background:var(--white); border-radius:16px; width:100%; max-width:720px; max-height:90vh; overflow-y:auto; }
  .modal-header { padding:20px 24px 16px; border-bottom:1px solid var(--border); display:flex; align-items:flex-start; justify-content:space-between; gap:12px; position:sticky; top:0; background:var(--white); z-index:1; }
  .modal-title { font-family:Cambria,serif; font-size:20px; color:var(--dark); }
  .modal-sub { font-size:13px; color:var(--muted); margin-top:2px; }
  .modal-close { background:none; border:none; font-size:22px; color:var(--muted); cursor:pointer; line-height:1; padding:2px; }
  .modal-body { padding:20px 24px; }
  .modal-section { margin-bottom:24px; }
  .modal-section-title { font-size:13px; font-weight:700; text-transform:uppercase; letter-spacing:.5px; color:var(--muted); margin-bottom:12px; }
  .video-container { position:relative; padding-bottom:56.25%; height:0; overflow:hidden; border-radius:8px; background:#000; }
  .video-container iframe { position:absolute; top:0; left:0; width:100%; height:100%; border:none; }
  .video-placeholder { position:absolute; inset:0; display:flex; flex-direction:column; align-items:center; justify-content:center; gap:8px; color:#6B7280; }
  .video-placeholder svg { opacity:.4; }
  .doc-link { display:flex; align-items:center; gap:10px; padding:10px 14px; border:1px solid var(--border); border-radius:8px; text-decoration:none; color:var(--dark); transition:all .15s; margin-bottom:8px; }
  .doc-link:hover { border-color:var(--blue); background:var(--blue-light); }
  .doc-link-icon { width:32px; height:32px; background:var(--blue-light); border-radius:6px; display:flex; align-items:center; justify-content:center; color:var(--blue); font-size:16px; flex-shrink:0; }
  .doc-link-text { flex:1; }
  .doc-link-name { font-size:14px; font-weight:600; }
  .doc-link-type { font-size:12px; color:var(--muted); }
  .notes-box { background:var(--offwhite); border-radius:8px; padding:14px; font-size:14px; line-height:1.6; white-space:pre-wrap; color:var(--dark); }
  .notes-empty { color:var(--muted); font-style:italic; }

  /* ── Mentor input fields in modal ── */
  .mentor-field { margin-bottom:12px; }
  .mentor-field label { display:block; font-size:12px; font-weight:600; color:var(--muted); margin-bottom:5px; }
  .mentor-field input, .mentor-field textarea { width:100%; padding:9px 12px; border:1.5px solid var(--border); border-radius:7px; font-size:14px; outline:none; font-family:inherit; resize:vertical; }
  .mentor-field input:focus, .mentor-field textarea:focus { border-color:var(--blue); }
  .mentor-actions { display:flex; gap:8px; flex-wrap:wrap; }

  /* ── Coach list (mentor/admin view) ── */
  .coach-row { display:flex; align-items:center; gap:14px; padding:14px; border:1px solid var(--border); border-radius:var(--radius); background:var(--white); margin-bottom:10px; cursor:pointer; transition:all .15s; }
  .coach-row:hover { border-color:var(--blue); box-shadow:0 2px 8px rgba(52,120,235,.1); }
  .coach-avatar { width:40px; height:40px; border-radius:10px; background:var(--blue); display:flex; align-items:center; justify-content:center; color:var(--white); font-weight:700; font-size:16px; flex-shrink:0; }
  .coach-info { flex:1; min-width:0; }
  .coach-name { font-weight:700; font-size:15px; color:var(--dark); }
  .coach-meta { font-size:12px; color:var(--muted); margin-top:2px; }
  .progress-bar { height:4px; background:#E5E7EB; border-radius:2px; margin-top:8px; }
  .progress-fill { height:100%; background:var(--blue); border-radius:2px; transition:width .3s; }
  .coach-phase { font-size:12px; color:var(--blue); font-weight:600; }

  /* ── Admin: create user ── */
  .form-row { display:grid; grid-template-columns:1fr 1fr; gap:12px; }
  .form-row.three { grid-template-columns:1fr 1fr 1fr; }
  select { width:100%; padding:10px 14px; border:1.5px solid var(--border); border-radius:8px; font-size:14px; background:var(--white); outline:none; }
  select:focus { border-color:var(--blue); }

  /* ── Status pills ── */
  .pill { display:inline-flex; align-items:center; gap:4px; padding:3px 8px; border-radius:20px; font-size:11px; font-weight:600; }
  .pill-green { background:#ECFDF5; color:#059669; }
  .pill-blue { background:var(--blue-light); color:var(--blue); }
  .pill-grey { background:#F3F4F6; color:var(--locked); }
  .pill-orange { background:#FEF3C7; color:#D97706; }

  /* ── Loading ── */
  .loading { display:flex; align-items:center; justify-content:center; padding:60px; color:var(--muted); gap:10px; }
  .spinner { width:20px; height:20px; border:2px solid var(--border); border-top-color:var(--blue); border-radius:50%; animation:spin .7s linear infinite; }
  @keyframes spin { to { transform:rotate(360deg); } }

  /* ── Empty state ── */
  .empty { text-align:center; padding:48px 20px; color:var(--muted); }
  .empty h3 { font-family:Cambria,serif; font-size:18px; margin-bottom:8px; color:var(--dark); }

  /* ── Toast ── */
  #toast { position:fixed; bottom:24px; right:24px; background:var(--dark); color:var(--white); padding:12px 18px; border-radius:8px; font-size:14px; z-index:999; transform:translateY(80px); opacity:0; transition:all .25s; pointer-events:none; }
  #toast.show { transform:translateY(0); opacity:1; }

  @media (max-width:800px) {
    .form-row, .form-row.three { grid-template-columns:1fr; }
    .modal { border-radius:12px; }
    .phase-grid { grid-template-columns:1fr; }
    #auth-screen .hero-grid { grid-template-columns:1fr !important; }
    #auth-screen header { padding:24px 20px 0; }
    #auth-screen footer { padding:16px 20px; flex-direction:column; gap:4px; text-align:center; }
  }
</style>
</head>
<body>

<!-- Auth / Landing -->
<div id="auth-screen">
<style>
.lp-wrap { min-height:100vh; background:var(--dark); display:flex; flex-direction:column; padding-top:6px; }
.lp-bar { background:var(--blue); height:6px; position:fixed; top:0; left:0; right:0; z-index:20; }
.lp-header { padding:24px 20px; display:flex; align-items:center; gap:12px; border-bottom:1px solid rgba(255,255,255,.06); }
.lp-mark { width:40px; height:40px; background:var(--blue); border-radius:9px; display:flex; align-items:center; justify-content:center; flex-shrink:0; }
.lp-mark span { color:var(--white); font-weight:800; font-size:18px; font-family:Cambria,serif; }
.lp-gym { color:var(--white); font-family:Cambria,serif; font-size:16px; font-weight:700; line-height:1.2; }
.lp-sub { color:#8BA8D4; font-size:11px; letter-spacing:1.5px; text-transform:uppercase; margin-top:2px; }
.lp-body { flex:1; padding:28px 20px 0; max-width:520px; margin:0 auto; width:100%; }
.lp-tag { display:inline-block; background:var(--blue); color:var(--white); font-size:10px; font-weight:700; letter-spacing:2px; text-transform:uppercase; padding:4px 10px; border-radius:4px; margin-bottom:16px; }
.lp-h1 { font-family:Cambria,serif; font-size:32px; line-height:1.2; color:var(--white); margin-bottom:12px; }
.lp-desc { color:#8BA8D4; font-size:15px; line-height:1.65; margin-bottom:28px; }
.lp-card { background:rgba(255,255,255,.05); border:1px solid rgba(255,255,255,.10); border-radius:14px; padding:24px; margin-bottom:24px; }
.lp-card-bar { width:3px; height:26px; background:var(--blue); border-radius:2px; margin-bottom:16px; }
.lp-card h2 { font-family:Cambria,serif; font-size:22px; color:var(--white); margin-bottom:6px; }
.lp-card p { color:#8BA8D4; font-size:13px; line-height:1.5; margin-bottom:20px; }
.lp-label { display:block; font-size:11px; font-weight:700; color:#8BA8D4; letter-spacing:.8px; text-transform:uppercase; margin-bottom:7px; }
.lp-input { width:100%; padding:13px 15px; background:rgba(255,255,255,.07); border:1.5px solid rgba(255,255,255,.12); border-radius:8px; font-size:16px; color:var(--white); outline:none; font-family:Calibri,sans-serif; letter-spacing:1px; -webkit-text-security:disc; transition:border .2s; }
.lp-input:focus { border-color:var(--blue); }
.lp-btn { width:100%; padding:13px; background:var(--blue); color:var(--white); border:none; border-radius:8px; font-size:15px; font-weight:700; cursor:pointer; margin-top:14px; font-family:Calibri,sans-serif; letter-spacing:.3px; transition:background .15s; }
.lp-btn:hover { background:var(--blue-dark); }
.lp-err { display:none; color:#F87171; font-size:13px; margin-top:8px; text-align:center; }
.lp-hint { margin-top:20px; padding-top:16px; border-top:1px solid rgba(255,255,255,.07); text-align:center; color:#4B6A8A; font-size:12px; line-height:1.6; }
.lp-phases { margin-bottom:24px; }
.lp-phases-title { font-size:11px; font-weight:700; color:#4B6A8A; letter-spacing:1.5px; text-transform:uppercase; margin-bottom:12px; }
.lp-phase { display:flex; align-items:center; gap:12px; padding:10px 0; border-bottom:1px solid rgba(255,255,255,.05); }
.lp-phase:last-child { border-bottom:none; }
.lp-phase-num { width:30px; height:30px; background:var(--blue); border-radius:7px; display:flex; align-items:center; justify-content:center; font-weight:800; color:var(--white); font-size:13px; flex-shrink:0; }
.lp-phase-title { color:var(--white); font-size:13px; font-weight:600; line-height:1.3; }
.lp-phase-weeks { color:#4B6A8A; font-size:11px; margin-top:2px; }
.lp-values { display:flex; gap:16px; flex-wrap:wrap; margin-bottom:28px; }
.lp-value { display:flex; align-items:center; gap:6px; }
.lp-value-dot { width:5px; height:5px; background:var(--blue); border-radius:50%; }
.lp-value-text { color:#8BA8D4; font-size:13px; font-weight:600; }
.lp-footer { padding:20px; text-align:center; border-top:1px solid rgba(255,255,255,.05); margin-top:8px; }
.lp-footer span { color:#2D3748; font-size:11px; }

/* Desktop: side-by-side */
@media (min-width:900px) {
  .lp-body { max-width:1000px; display:grid; grid-template-columns:1fr 400px; gap:60px; align-items:start; padding-top:48px; }
  .lp-h1 { font-size:42px; }
  .lp-header { padding:28px 40px; }
  .lp-footer { text-align:left; padding:20px 40px; display:flex; justify-content:space-between; }
}
</style>

<div class="lp-bar"></div>
<div class="lp-wrap">

  <header class="lp-header">
    <div class="lp-mark"><span>R</span></div>
    <div>
      <div class="lp-gym">REBUILD HEALTH & FITNESS</div>
      <div class="lp-sub">Coach Development Program</div>
    </div>
  </header>

  <div class="lp-body">

    <!-- Left col: program info -->
    <div class="lp-left">
      <div class="lp-tag">12-Week Mentorship</div>
      <h1 class="lp-h1">Becoming a<br>Rebuild Coach</h1>
      <p class="lp-desc">A structured development journey through coaching practice, communication and leadership, injury management, and Rebuild's standards of excellence — guided by an experienced mentor.</p>

      <div class="lp-values">
        <div class="lp-value"><div class="lp-value-dot"></div><span class="lp-value-text">Integrity</span></div>
        <div class="lp-value"><div class="lp-value-dot"></div><span class="lp-value-text">Happiness</span></div>
        <div class="lp-value"><div class="lp-value-dot"></div><span class="lp-value-text">Progress</span></div>
      </div>

      <div class="lp-phases">
        <div class="lp-phases-title">Program Phases</div>
        <div class="lp-phase"><div class="lp-phase-num">1</div><div><div class="lp-phase-title">Orientation & Self-Assessment</div><div class="lp-phase-weeks">Weeks 1–2</div></div></div>
        <div class="lp-phase"><div class="lp-phase-num">2</div><div><div class="lp-phase-title">Knowledge & Physical Capabilities</div><div class="lp-phase-weeks">Weeks 3–4</div></div></div>
        <div class="lp-phase"><div class="lp-phase-num">3</div><div><div class="lp-phase-title">Coaching, Communication & Leadership</div><div class="lp-phase-weeks">Weeks 5–6</div></div></div>
        <div class="lp-phase"><div class="lp-phase-num">4</div><div><div class="lp-phase-title">Injury & Pain Triage</div><div class="lp-phase-weeks">Weeks 7–8</div></div></div>
        <div class="lp-phase"><div class="lp-phase-num">5</div><div><div class="lp-phase-title">World-Best-Standard Practices</div><div class="lp-phase-weeks">Weeks 9–10</div></div></div>
        <div class="lp-phase"><div class="lp-phase-num">6</div><div><div class="lp-phase-title">Evaluation & Graduation</div><div class="lp-phase-weeks">Weeks 11–12</div></div></div>
      </div>
    </div>

    <!-- Right col / mobile-first: login card -->
    <div class="lp-right">
      <div class="lp-card">
        <div class="lp-card-bar"></div>
        <h2>Sign in</h2>
        <p>Enter your access code to open your development dashboard.</p>
        <label class="lp-label" for="access-code-input">Access Code</label>
        <input class="lp-input" type="password" id="access-code-input" placeholder="Enter your code" autocomplete="off">
        <button class="lp-btn" onclick="handleLogin()">Enter Dashboard →</button>
        <p class="lp-err" id="auth-error">Invalid access code. Please try again.</p>
        <div class="lp-hint">Don't have a code?<br>Contact your program administrator.</div>
      </div>
    </div>

  </div>

  <footer class="lp-footer">
    <span>© Rebuild Health & Fitness · Coach Development Program</span>
    <span>Capalaba · Queensland</span>
  </footer>

</div>
</div>

<!-- App -->
<div id="app">
  <div class="topbar">
    <div class="topbar-logo">
      <div class="topbar-mark"><span>R</span></div>
      <span class="topbar-name">Coach Development</span>
    </div>
    <div class="topbar-right">
      <span class="role-badge" id="role-badge"></span>
      <span class="topbar-user" id="topbar-user"></span>
      <button class="logout-btn" onclick="handleLogout()">Sign out</button>
    </div>
  </div>

  <div class="nav-tabs" id="nav-tabs"></div>

  <div class="main" id="main-content">
    <div class="loading"><div class="spinner"></div> Loading…</div>
  </div>
</div>

<!-- Session Modal -->
<div class="modal-overlay" id="session-modal">
  <div class="modal">
    <div class="modal-header">
      <div>
        <div class="modal-title" id="modal-title"></div>
        <div class="modal-sub" id="modal-sub"></div>
      </div>
      <button class="modal-close" onclick="closeModal()">×</button>
    </div>
    <div class="modal-body" id="modal-body"></div>
  </div>
</div>

<div id="toast"></div>

<script>
// ═══════════════════════════════════════════════════════════════════════
// PROGRAM STRUCTURE
// ═══════════════════════════════════════════════════════════════════════
const PHASES = [
  { id:1, title:"Orientation & Self-Assessment", weeks:"Weeks 1–2", unlockDay:0,
    sessions:[
      { id:"1a", title:"Introduction & Shadowing" },
      { id:"1b", title:"Heatmap Self-Assessment" },
      { id:"1c", title:"Goal Setting & IDP" },
    ]},
  { id:2, title:"Knowledge & Physical Capabilities", weeks:"Weeks 3–4", unlockDay:14,
    sessions:[
      { id:"2a", title:"Learning, Knowledge & Experience" },
      { id:"2b", title:"Personal Physical Capabilities" },
      { id:"2c", title:"Business Integration" },
    ]},
  { id:3, title:"Coaching, Communication & Leadership", weeks:"Weeks 5–6", unlockDay:28,
    sessions:[
      { id:"3a", title:"Coaching Practice & Feedback" },
      { id:"3b", title:"Leadership & Communication Skills" },
      { id:"3c", title:"Gym Operations" },
    ]},
  { id:4, title:"Injury & Pain Triage", weeks:"Weeks 7–8", unlockDay:42,
    sessions:[
      { id:"4a", title:"Practical Observations" },
      { id:"4b", title:"Referral Network & Collaboration" },
      { id:"4c", title:"Business Risk Management" },
    ]},
  { id:5, title:"World-Best-Standard Practices", weeks:"Weeks 9–10", unlockDay:56,
    sessions:[
      { id:"5a", title:"Advanced Coaching Standards" },
      { id:"5b", title:"Programming & Periodisation" },
      { id:"5c", title:"Member Development" },
    ]},
  { id:6, title:"Evaluation & Graduation", weeks:"Weeks 11–12", unlockDay:70,
    sessions:[
      { id:"6a", title:"Heatmap Re-Assessment" },
      { id:"6b", title:"Portfolio Review" },
      { id:"6c", title:"Graduation & Next Steps" },
    ]},
];

// ═══════════════════════════════════════════════════════════════════════
// STATE
// ═══════════════════════════════════════════════════════════════════════
let db = null, user = null;
let currentUser = null; // { name, role, coachId, code }
let currentTab = null;
let selectedCoachId = null;

// ═══════════════════════════════════════════════════════════════════════
// INIT
// ═══════════════════════════════════════════════════════════════════════
async function init() {
  try {
    [db, user] = await Promise.all([
      claude.use("db"),
      claude.use("user"),
    ]);
  } catch(e) {
    db = null; user = null;
  }

  // Check if already logged in via sessionStorage
  const saved = sessionStorage.getItem("rebuild_user");
  if (saved) {
    try {
      currentUser = JSON.parse(saved);
      showApp();
      return;
    } catch(e) {}
  }

  document.getElementById("access-code-input").addEventListener("keydown", e => {
    if (e.key === "Enter") handleLogin();
  });
}

// ═══════════════════════════════════════════════════════════════════════
// AUTH
// ═══════════════════════════════════════════════════════════════════════
async function handleLogin() {
  const code = document.getElementById("access-code-input").value.trim().toUpperCase();
  if (!code) return;

  const errEl = document.getElementById("auth-error");
  errEl.style.display = "none";

  // Look up code in db
  if (!db) { errEl.textContent = "Database unavailable."; errEl.style.display="block"; return; }

  try {
    const snap = await db.collection("users").where("code","==",code).limit(1).get();
    if (snap.empty) { errEl.style.display="block"; return; }
    const doc = snap.docs[0];
    currentUser = { id: doc.id, ...doc.data() };
    sessionStorage.setItem("rebuild_user", JSON.stringify(currentUser));
    showApp();
  } catch(e) {
    errEl.textContent = "Error signing in. Please try again.";
    errEl.style.display="block";
  }
}

function handleLogout() {
  sessionStorage.removeItem("rebuild_user");
  currentUser = null;
  document.getElementById("auth-screen").style.display = "flex";
  document.getElementById("app").style.display = "none";
  document.getElementById("access-code-input").value = "";
}

// ═══════════════════════════════════════════════════════════════════════
// APP SHELL
// ═══════════════════════════════════════════════════════════════════════
function showApp() {
  document.getElementById("auth-screen").style.display = "none";
  document.getElementById("app").style.display = "block";

  // Role badge
  const badge = document.getElementById("role-badge");
  badge.textContent = currentUser.role;
  badge.className = "role-badge role-" + currentUser.role;
  document.getElementById("topbar-user").textContent = currentUser.name;

  // Build nav
  buildNav();
}

function buildNav() {
  const nav = document.getElementById("nav-tabs");
  let tabs = [];
  if (currentUser.role === "coach") {
    tabs = [
      { id:"my-program", label:"My Program" },
      { id:"my-heatmap", label:"Heatmap" },
    ];
  } else if (currentUser.role === "mentor") {
    tabs = [
      { id:"my-coaches", label:"My Coaches" },
      { id:"my-program", label:"My Program" },
    ];
  } else if (currentUser.role === "admin") {
    tabs = [
      { id:"all-coaches", label:"All Coaches" },
      { id:"manage-users", label:"Manage Users" },
      { id:"my-program", label:"My Program" },
    ];
  }

  nav.innerHTML = tabs.map(t =>
    `<div class="nav-tab" data-tab="${t.id}" onclick="switchTab('${t.id}')">${t.label}</div>`
  ).join("");

  switchTab(tabs[0].id);
}

function switchTab(tabId) {
  currentTab = tabId;
  document.querySelectorAll(".nav-tab").forEach(t => {
    t.classList.toggle("active", t.dataset.tab === tabId);
  });
  renderTab(tabId);
}

async function renderTab(tabId) {
  const main = document.getElementById("main-content");
  main.innerHTML = `<div class="loading"><div class="spinner"></div> Loading…</div>`;

  if (tabId === "my-program") {
    await renderMyProgram(main, currentUser.id);
  } else if (tabId === "my-heatmap") {
    await renderHeatmap(main, currentUser.id);
  } else if (tabId === "my-coaches") {
    await renderMentorCoaches(main);
  } else if (tabId === "all-coaches") {
    await renderAllCoaches(main);
  } else if (tabId === "manage-users") {
    await renderManageUsers(main);
  }
}

// ═══════════════════════════════════════════════════════════════════════
// COACH: MY PROGRAM
// ═══════════════════════════════════════════════════════════════════════
async function renderMyProgram(container, coachId) {
  let coachData = {};
  try {
    const snap = await db.doc(`coach_data/${coachId}`).get();
    if (snap.exists) coachData = snap.data();
  } catch(e) {}

  const enrolledDate = coachData.enrolledDate ? new Date(coachData.enrolledDate) : null;
  const today = new Date();

  let html = `<div style="margin-bottom:20px">
    <h2 style="font-family:Cambria,serif;font-size:22px;color:var(--dark)">Your 12-Week Program</h2>
    <p style="color:var(--muted);font-size:14px;margin-top:4px">
      ${enrolledDate ? `Enrolled ${enrolledDate.toLocaleDateString("en-AU",{day:"numeric",month:"long",year:"numeric"})}` : "Not yet enrolled"}
    </p>
  </div>
  <div class="phase-grid">`;

  for (const phase of PHASES) {
    const daysElapsed = enrolledDate ? Math.floor((today - enrolledDate) / 86400000) : -1;
    const autoUnlocked = enrolledDate && daysElapsed >= phase.unlockDay;
    const manualUnlocked = coachData[`phase${phase.id}_unlocked`] === true;
    const isUnlocked = autoUnlocked || manualUnlocked;
    const unlockDateStr = enrolledDate
      ? new Date(enrolledDate.getTime() + phase.unlockDay*86400000).toLocaleDateString("en-AU",{day:"numeric",month:"short"})
      : null;

    html += `<div class="phase-card ${isUnlocked?"phase-unlocked":"phase-locked"}">
      <div class="phase-header">
        <div class="phase-badge">${phase.id}</div>
        <div>
          <div class="phase-title">${phase.title}</div>
          <div class="phase-weeks">${phase.weeks}</div>
        </div>
      </div>
      <div class="phase-body">
        <ul class="sub-list">`;

    for (const session of phase.sessions) {
      const hasVideo = coachData[`${session.id}_video`];
      const hasDocs = coachData[`${session.id}_docs`];
      if (isUnlocked) {
        html += `<li class="sub-item sub-unlocked" onclick="openSession('${session.id}','${session.title}','${coachId}')">
          <div class="sub-icon">▶</div>
          <span class="sub-name">${session.id.toUpperCase()} — ${session.title}</span>
          <span class="sub-status">${hasVideo ? '<span class="pill pill-green">Video</span>' : '<span class="pill pill-grey">No video</span>'}</span>
        </li>`;
      } else {
        html += `<li class="sub-item sub-locked">
          <div class="sub-icon">🔒</div>
          <span class="sub-name">${session.id.toUpperCase()} — ${session.title}</span>
        </li>`;
      }
    }

    html += `</ul>`;
    if (!isUnlocked && unlockDateStr) {
      html += `<div class="lock-date">🔒 Unlocks ${unlockDateStr}</div>`;
    }
    html += `</div></div>`;
  }

  html += `</div>`;
  container.innerHTML = html;
}

// ═══════════════════════════════════════════════════════════════════════
// SESSION MODAL
// ═══════════════════════════════════════════════════════════════════════
async function openSession(sessionId, sessionTitle, coachId, isMentorView) {
  const modal = document.getElementById("session-modal");
  const phase = PHASES.find(p => p.sessions.find(s => s.id === sessionId));
  document.getElementById("modal-title").textContent = `${sessionId.toUpperCase()} — ${sessionTitle}`;
  document.getElementById("modal-sub").textContent = phase ? `Phase ${phase.id}: ${phase.title}` : "";

  const body = document.getElementById("modal-body");
  body.innerHTML = `<div class="loading"><div class="spinner"></div> Loading…</div>`;
  modal.classList.add("open");

  let coachData = {};
  try {
    const snap = await db.doc(`coach_data/${coachId}`).get();
    if (snap.exists) coachData = snap.data();
  } catch(e) {}

  const videoUrl = coachData[`${sessionId}_video`] || "";
  const docs = coachData[`${sessionId}_docs`] || [];
  const notes = coachData[`${sessionId}_notes`] || "";
  const canEdit = currentUser.role === "mentor" || currentUser.role === "admin";

  let html = "";

  // Video section
  html += `<div class="modal-section">
    <div class="modal-section-title">Presentation Video</div>`;

  if (videoUrl) {
    const embedUrl = driveEmbedUrl(videoUrl);
    html += `<div class="video-container">
      <iframe src="${embedUrl}" allowfullscreen allow="autoplay"></iframe>
    </div>`;
  } else {
    html += `<div class="video-container">
      <div class="video-placeholder">
        <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="12" cy="12" r="10"/><polygon points="10,8 16,12 10,16"/></svg>
        <span>No video uploaded yet</span>
      </div>
    </div>`;
  }
  html += `</div>`;

  // Documents
  if (docs.length > 0) {
    html += `<div class="modal-section">
      <div class="modal-section-title">Documents & Resources</div>`;
    docs.forEach(doc => {
      html += `<a class="doc-link" href="${doc.url}" target="_blank" rel="noopener">
        <div class="doc-link-icon">📄</div>
        <div class="doc-link-text">
          <div class="doc-link-name">${doc.name}</div>
          <div class="doc-link-type">Google Drive</div>
        </div>
        <span style="color:var(--blue);font-size:12px">Open ↗</span>
      </a>`;
    });
    html += `</div>`;
  }

  // Mentor notes
  html += `<div class="modal-section">
    <div class="modal-section-title">Mentor Notes</div>
    ${notes
      ? `<div class="notes-box">${escHtml(notes)}</div>`
      : `<div class="notes-box notes-empty">No mentor notes for this session yet.</div>`
    }
  </div>`;

  // Mentor edit controls
  if (canEdit) {
    html += `<div class="modal-section">
      <div class="modal-section-title">Mentor Controls</div>
      <div class="mentor-field">
        <label>Video Link (Google Drive share link)</label>
        <input type="url" id="edit-video" value="${escHtml(videoUrl)}" placeholder="https://drive.google.com/file/d/...">
      </div>
      <div id="docs-editor">`;

    docs.forEach((doc, i) => {
      html += `<div style="display:flex;gap:8px;margin-bottom:8px" id="doc-row-${i}">
        <input type="text" placeholder="Document name" value="${escHtml(doc.name)}" id="doc-name-${i}" style="flex:1;padding:8px 10px;border:1.5px solid var(--border);border-radius:7px;font-size:13px;">
        <input type="url" placeholder="Google Drive link" value="${escHtml(doc.url)}" id="doc-url-${i}" style="flex:2;padding:8px 10px;border:1.5px solid var(--border);border-radius:7px;font-size:13px;">
        <button class="btn btn-sm btn-danger" onclick="removeDocRow(${i})">✕</button>
      </div>`;
    });

    html += `</div>
      <button class="btn btn-sm btn-ghost" onclick="addDocRow()" style="margin-bottom:12px">+ Add Document</button>
      <div class="mentor-field">
        <label>Mentor Notes</label>
        <textarea id="edit-notes" rows="4" placeholder="Add notes for this coach about this session…">${escHtml(notes)}</textarea>
      </div>
      <div class="mentor-actions">
        <button class="btn btn-primary btn-sm" onclick="saveSession('${sessionId}','${coachId}')">Save Changes</button>
      </div>
    </div>`;
  }

  body.innerHTML = html;
  window._docCount = docs.length;
}

let _docCount = 0;

function addDocRow() {
  const editor = document.getElementById("docs-editor");
  const i = _docCount++;
  const row = document.createElement("div");
  row.style.cssText = "display:flex;gap:8px;margin-bottom:8px";
  row.id = `doc-row-${i}`;
  row.innerHTML = `
    <input type="text" placeholder="Document name" id="doc-name-${i}" style="flex:1;padding:8px 10px;border:1.5px solid var(--border);border-radius:7px;font-size:13px;">
    <input type="url" placeholder="Google Drive link" id="doc-url-${i}" style="flex:2;padding:8px 10px;border:1.5px solid var(--border);border-radius:7px;font-size:13px;">
    <button class="btn btn-sm btn-danger" onclick="removeDocRow(${i})">✕</button>`;
  editor.appendChild(row);
}

function removeDocRow(i) {
  const row = document.getElementById(`doc-row-${i}`);
  if (row) row.remove();
}

async function saveSession(sessionId, coachId) {
  const videoUrl = document.getElementById("edit-video")?.value?.trim() || "";
  const notes = document.getElementById("edit-notes")?.value?.trim() || "";

  // Collect docs
  const docs = [];
  document.querySelectorAll("[id^='doc-row-']").forEach(row => {
    const i = row.id.replace("doc-row-","");
    const name = document.getElementById(`doc-name-${i}`)?.value?.trim();
    const url = document.getElementById(`doc-url-${i}`)?.value?.trim();
    if (name && url) docs.push({ name, url });
  });

  try {
    await db.doc(`coach_data/${coachId}`).update({
      [`${sessionId}_video`]: videoUrl,
      [`${sessionId}_docs`]: docs,
      [`${sessionId}_notes`]: notes,
    });
    showToast("Saved ✓");
  } catch(e) {
    // Try set if update fails (doc may not exist)
    try {
      const snap = await db.doc(`coach_data/${coachId}`).get();
      const existing = snap.exists ? snap.data() : {};
      await db.doc(`coach_data/${coachId}`).set({
        ...existing,
        [`${sessionId}_video`]: videoUrl,
        [`${sessionId}_docs`]: docs,
        [`${sessionId}_notes`]: notes,
      });
      showToast("Saved ✓");
    } catch(e2) {
      showToast("Error saving — " + e2.message);
    }
  }
}

function closeModal() {
  document.getElementById("session-modal").classList.remove("open");
  // Refresh current tab
  if (currentTab) renderTab(currentTab);
}

// ═══════════════════════════════════════════════════════════════════════
// HEATMAP VIEW
// ═══════════════════════════════════════════════════════════════════════
async function renderHeatmap(container, coachId) {
  let coachData = {};
  try {
    const snap = await db.doc(`coach_data/${coachId}`).get();
    if (snap.exists) coachData = snap.data();
  } catch(e) {}

  const heatmap2 = coachData.heatmap_week2 || "";
  const heatmap11 = coachData.heatmap_week11 || "";

  container.innerHTML = `
    <div style="margin-bottom:20px">
      <h2 style="font-family:Cambria,serif;font-size:22px;color:var(--dark)">Coaching Competency Heatmap</h2>
      <p style="color:var(--muted);font-size:14px;margin-top:4px">Your self-assessment snapshots across the 12-week program.</p>
    </div>
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px">
      <div class="card">
        <div class="card-header"><div><div class="card-title">Week 2 Baseline</div><div class="card-sub">Initial self-assessment</div></div></div>
        ${heatmap2
          ? `<a class="doc-link" href="${heatmap2}" target="_blank"><div class="doc-link-icon">📊</div><div class="doc-link-text"><div class="doc-link-name">Heatmap — Week 2</div><div class="doc-link-type">Google Drive</div></div><span style="color:var(--blue);font-size:12px">Open ↗</span></a>`
          : `<p style="color:var(--muted);font-size:14px">Not yet uploaded by your mentor.</p>`}
      </div>
      <div class="card">
        <div class="card-header"><div><div class="card-title">Week 11 Re-Assessment</div><div class="card-sub">Program completion</div></div></div>
        ${heatmap11
          ? `<a class="doc-link" href="${heatmap11}" target="_blank"><div class="doc-link-icon">📊</div><div class="doc-link-text"><div class="doc-link-name">Heatmap — Week 11</div><div class="doc-link-type">Google Drive</div></div><span style="color:var(--blue);font-size:12px">Open ↗</span></a>`
          : `<p style="color:var(--muted);font-size:14px">Will be available at Week 11.</p>`}
      </div>
    </div>`;
}

// ═══════════════════════════════════════════════════════════════════════
// MENTOR: MY COACHES
// ═══════════════════════════════════════════════════════════════════════
async function renderMentorCoaches(container) {
  let coaches = [];
  try {
    const snap = await db.collection("users").where("mentorId","==",currentUser.id).where("role","==","coach").get();
    coaches = snap.docs.map(d => ({ id:d.id, ...d.data() }));
  } catch(e) {}

  if (coaches.length === 0) {
    container.innerHTML = `<div class="empty"><h3>No coaches assigned yet</h3><p>Ask your administrator to assign coaches to you.</p></div>`;
    return;
  }

  let html = `<div style="margin-bottom:20px">
    <h2 style="font-family:Cambria,serif;font-size:22px;color:var(--dark)">My Coaches</h2>
    <p style="color:var(--muted);font-size:14px;margin-top:4px">${coaches.length} coach${coaches.length!==1?"es":""} assigned to you</p>
  </div>`;

  for (const coach of coaches) {
    html += await renderCoachRow(coach, true);
  }

  container.innerHTML = html;
}

// ═══════════════════════════════════════════════════════════════════════
// ADMIN: ALL COACHES
// ═══════════════════════════════════════════════════════════════════════
async function renderAllCoaches(container) {
  let coaches = [], mentors = [];
  try {
    const [cSnap, mSnap] = await Promise.all([
      db.collection("users").where("role","==","coach").get(),
      db.collection("users").where("role","in",["mentor","admin"]).get(),
    ]);
    coaches = cSnap.docs.map(d=>({id:d.id,...d.data()}));
    mentors = mSnap.docs.map(d=>({id:d.id,...d.data()}));
  } catch(e) {}

  const mentorMap = {};
  mentors.forEach(m => mentorMap[m.id] = m.name);

  let html = `<div style="margin-bottom:20px">
    <h2 style="font-family:Cambria,serif;font-size:22px;color:var(--dark)">All Coaches</h2>
    <p style="color:var(--muted);font-size:14px;margin-top:4px">${coaches.length} coach${coaches.length!==1?"es":""} across both gyms</p>
  </div>`;

  if (coaches.length === 0) {
    html += `<div class="empty"><h3>No coaches yet</h3><p>Create coach accounts in Manage Users.</p></div>`;
  } else {
    for (const coach of coaches) {
      html += await renderCoachRow(coach, true, mentorMap[coach.mentorId]);
    }
  }

  container.innerHTML = html;
}

async function renderCoachRow(coach, clickable, mentorName) {
  let coachData = {};
  try {
    const snap = await db.doc(`coach_data/${coach.id}`).get();
    if (snap.exists) coachData = snap.data();
  } catch(e) {}

  const enrolledDate = coachData.enrolledDate ? new Date(coachData.enrolledDate) : null;
  const today = new Date();
  const daysElapsed = enrolledDate ? Math.floor((today-enrolledDate)/86400000) : -1;

  // Calculate progress
  let unlockedCount = 0;
  PHASES.forEach(p => {
    const auto = enrolledDate && daysElapsed >= p.unlockDay;
    const manual = coachData[`phase${p.id}_unlocked`] === true;
    if (auto || manual) unlockedCount++;
  });
  const pct = Math.round((unlockedCount/6)*100);

  const initials = coach.name.split(" ").map(w=>w[0]).join("").toUpperCase().slice(0,2);
  const gym = coach.gym || "—";
  const meta = [gym, mentorName ? `Mentor: ${mentorName}` : null, enrolledDate ? `Enrolled ${enrolledDate.toLocaleDateString("en-AU",{day:"numeric",month:"short"})}` : "Not enrolled"].filter(Boolean).join(" · ");

  return `<div class="coach-row" onclick="openCoachView('${coach.id}','${escHtml(coach.name)}')">
    <div class="coach-avatar">${initials}</div>
    <div class="coach-info">
      <div class="coach-name">${escHtml(coach.name)}</div>
      <div class="coach-meta">${escHtml(meta)}</div>
      <div class="progress-bar"><div class="progress-fill" style="width:${pct}%"></div></div>
    </div>
    <div class="coach-phase">${unlockedCount}/6 phases</div>
  </div>`;
}

async function openCoachView(coachId, coachName) {
  selectedCoachId = coachId;
  const main = document.getElementById("main-content");
  main.innerHTML = `<div class="loading"><div class="spinner"></div> Loading…</div>`;

  let coachData = {};
  try {
    const snap = await db.doc(`coach_data/${coachId}`).get();
    if (snap.exists) coachData = snap.data();
  } catch(e) {}

  const enrolledDate = coachData.enrolledDate ? new Date(coachData.enrolledDate) : null;
  const today = new Date();
  const daysElapsed = enrolledDate ? Math.floor((today-enrolledDate)/86400000) : -1;

  let html = `<div style="margin-bottom:20px;display:flex;align-items:center;gap:12px">
    <button class="btn btn-ghost btn-sm" onclick="switchTab('${currentTab === "all-coaches" ? "all-coaches" : "my-coaches"}')">← Back</button>
    <div>
      <h2 style="font-family:Cambria,serif;font-size:22px;color:var(--dark)">${escHtml(coachName)}</h2>
      <p style="color:var(--muted);font-size:14px;margin-top:2px">${enrolledDate ? `Enrolled ${enrolledDate.toLocaleDateString("en-AU",{day:"numeric",month:"long",year:"numeric"})}` : "Not yet enrolled"}</p>
    </div>
  </div>`;

  // Enrolment + heatmap controls
  html += `<div class="card" style="margin-bottom:20px">
    <div class="card-header"><div class="card-title">Coach Settings</div></div>
    <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:12px;margin-bottom:12px">
      <div class="mentor-field">
        <label>Gym</label>
        <select id="coach-gym">
          <option value="Rebuild Capalaba" ${coachData.gym==="Rebuild Capalaba"?"selected":""}>Rebuild Capalaba</option>
          <option value="Rebels WC" ${coachData.gym==="Rebels WC"?"selected":""}>Rebels WC</option>
        </select>
      </div>
      <div class="mentor-field">
        <label>Enrolment Date</label>
        <input type="date" id="coach-enrolled" value="${coachData.enrolledDate||""}">
      </div>
      <div style="display:flex;align-items:flex-end">
        <button class="btn btn-primary btn-sm" onclick="saveCoachSettings('${coachId}')">Save Settings</button>
      </div>
    </div>
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px">
      <div class="mentor-field">
        <label>Heatmap Week 2 (Google Drive link)</label>
        <input type="url" id="coach-hm2" value="${escHtml(coachData.heatmap_week2||"")}" placeholder="https://drive.google.com/...">
      </div>
      <div class="mentor-field">
        <label>Heatmap Week 11 (Google Drive link)</label>
        <input type="url" id="coach-hm11" value="${escHtml(coachData.heatmap_week11||"")}" placeholder="https://drive.google.com/...">
      </div>
    </div>
    <button class="btn btn-primary btn-sm" onclick="saveHeatmaps('${coachId}')">Save Heatmaps</button>
  </div>`;

  // Phase grid
  html += `<div class="phase-grid">`;

  for (const phase of PHASES) {
    const auto = enrolledDate && daysElapsed >= phase.unlockDay;
    const manual = coachData[`phase${phase.id}_unlocked`] === true;
    const isUnlocked = auto || manual;
    const unlockDateStr = enrolledDate
      ? new Date(enrolledDate.getTime() + phase.unlockDay*86400000).toLocaleDateString("en-AU",{day:"numeric",month:"short"})
      : null;

    html += `<div class="phase-card ${isUnlocked?"phase-unlocked":"phase-locked"}">
      <div class="phase-header">
        <div class="phase-badge">${phase.id}</div>
        <div style="flex:1">
          <div class="phase-title">${phase.title}</div>
          <div class="phase-weeks">${phase.weeks}</div>
        </div>
        <button class="btn btn-sm ${isUnlocked?"btn-success":"btn-ghost"}" style="font-size:11px;padding:5px 10px"
          onclick="togglePhase('${coachId}',${phase.id},${isUnlocked})">
          ${isUnlocked ? "✓ Unlocked" : "Unlock"}
        </button>
      </div>
      <div class="phase-body">
        <ul class="sub-list">`;

    for (const session of phase.sessions) {
      const hasVideo = coachData[`${session.id}_video`];
      if (isUnlocked) {
        html += `<li class="sub-item sub-unlocked" onclick="openSession('${session.id}','${session.title}','${coachId}',true)">
          <div class="sub-icon">✏️</div>
          <span class="sub-name">${session.id.toUpperCase()} — ${session.title}</span>
          <span>${hasVideo ? '<span class="pill pill-green">Video</span>' : '<span class="pill pill-grey">No video</span>'}</span>
        </li>`;
      } else {
        html += `<li class="sub-item sub-locked">
          <div class="sub-icon">🔒</div>
          <span class="sub-name">${session.id.toUpperCase()} — ${session.title}</span>
        </li>`;
      }
    }

    html += `</ul>`;
    if (!isUnlocked && unlockDateStr) {
      html += `<div class="lock-date">Auto-unlocks ${unlockDateStr}</div>`;
    }
    html += `</div></div>`;
  }

  html += `</div>`;
  main.innerHTML = html;
}

async function togglePhase(coachId, phaseId, currentlyUnlocked) {
  try {
    const snap = await db.doc(`coach_data/${coachId}`).get();
    const existing = snap.exists ? snap.data() : {};
    await db.doc(`coach_data/${coachId}`).set({
      ...existing,
      [`phase${phaseId}_unlocked`]: !currentlyUnlocked,
    });
    showToast(currentlyUnlocked ? "Phase locked" : "Phase unlocked ✓");
    await openCoachView(coachId, "");
  } catch(e) { showToast("Error: " + e.message); }
}

async function saveCoachSettings(coachId) {
  const gym = document.getElementById("coach-gym")?.value || "";
  const enrolled = document.getElementById("coach-enrolled")?.value || "";
  try {
    const snap = await db.doc(`coach_data/${coachId}`).get();
    const existing = snap.exists ? snap.data() : {};
    await db.doc(`coach_data/${coachId}`).set({ ...existing, gym, enrolledDate: enrolled });
    // Also update users doc
    const uSnap = await db.doc(`users/${coachId}`).get();
    const uExisting = uSnap.exists ? uSnap.data() : {};
    await db.doc(`users/${coachId}`).set({ ...uExisting, gym });
    showToast("Settings saved ✓");
  } catch(e) { showToast("Error: " + e.message); }
}

async function saveHeatmaps(coachId) {
  const hm2 = document.getElementById("coach-hm2")?.value?.trim() || "";
  const hm11 = document.getElementById("coach-hm11")?.value?.trim() || "";
  try {
    const snap = await db.doc(`coach_data/${coachId}`).get();
    const existing = snap.exists ? snap.data() : {};
    await db.doc(`coach_data/${coachId}`).set({ ...existing, heatmap_week2: hm2, heatmap_week11: hm11 });
    showToast("Heatmaps saved ✓");
  } catch(e) { showToast("Error: " + e.message); }
}

// ═══════════════════════════════════════════════════════════════════════
// ADMIN: MANAGE USERS
// ═══════════════════════════════════════════════════════════════════════
async function renderManageUsers(container) {
  let users = [];
  try {
    const snap = await db.collection("users").get();
    users = snap.docs.map(d=>({id:d.id,...d.data()}));
  } catch(e) {}

  const mentors = users.filter(u=>u.role==="mentor"||u.role==="admin");

  let html = `<div style="margin-bottom:24px">
    <h2 style="font-family:Cambria,serif;font-size:22px;color:var(--dark);margin-bottom:16px">Manage Users</h2>

    <!-- Create user form -->
    <div class="card">
      <div class="card-title" style="margin-bottom:16px">Create New User</div>
      <div class="form-row three">
        <div class="field"><label>Full Name</label><input type="text" id="new-name" placeholder="Jane Smith"></div>
        <div class="field"><label>Access Code</label><input type="text" id="new-code" placeholder="REBUILD01" style="text-transform:uppercase"></div>
        <div class="field"><label>Role</label>
          <select id="new-role" onchange="toggleMentorField()">
            <option value="coach">Coach</option>
            <option value="mentor">Mentor</option>
            <option value="admin">Admin</option>
          </select>
        </div>
      </div>
      <div class="form-row">
        <div class="field"><label>Gym</label>
          <select id="new-gym">
            <option value="Rebuild Capalaba">Rebuild Capalaba</option>
            <option value="Rebels WC">Rebels WC</option>
          </select>
        </div>
        <div class="field" id="mentor-assign-field">
          <label>Assign Mentor</label>
          <select id="new-mentor">
            <option value="">— No mentor —</option>
            ${mentors.map(m=>`<option value="${m.id}">${escHtml(m.name)}</option>`).join("")}
          </select>
        </div>
      </div>
      <button class="btn btn-primary btn-sm" onclick="createUser()">Create User</button>
    </div>
  </div>

  <!-- User list -->
  <div class="card">
    <div class="card-title" style="margin-bottom:16px">All Users (${users.length})</div>
    <table style="width:100%;border-collapse:collapse;font-size:14px">
      <thead>
        <tr style="border-bottom:2px solid var(--border)">
          <th style="text-align:left;padding:8px 10px;color:var(--muted);font-size:12px;text-transform:uppercase;letter-spacing:.5px">Name</th>
          <th style="text-align:left;padding:8px 10px;color:var(--muted);font-size:12px;text-transform:uppercase;letter-spacing:.5px">Code</th>
          <th style="text-align:left;padding:8px 10px;color:var(--muted);font-size:12px;text-transform:uppercase;letter-spacing:.5px">Role</th>
          <th style="text-align:left;padding:8px 10px;color:var(--muted);font-size:12px;text-transform:uppercase;letter-spacing:.5px">Gym</th>
          <th style="padding:8px 10px"></th>
        </tr>
      </thead>
      <tbody>`;

  users.sort((a,b)=>a.name.localeCompare(b.name)).forEach(u => {
    html += `<tr style="border-bottom:1px solid var(--border)">
      <td style="padding:10px">${escHtml(u.name)}</td>
      <td style="padding:10px"><code style="background:var(--offwhite);padding:2px 7px;border-radius:4px;font-size:13px">${escHtml(u.code||"")}</code></td>
      <td style="padding:10px"><span class="role-badge role-${u.role}" style="font-size:10px">${u.role}</span></td>
      <td style="padding:10px;color:var(--muted)">${escHtml(u.gym||"")}</td>
      <td style="padding:10px"><button class="btn btn-danger btn-sm" onclick="deleteUser('${u.id}','${escHtml(u.name)}')">Remove</button></td>
    </tr>`;
  });

  html += `</tbody></table></div>`;
  container.innerHTML = html;
}

function toggleMentorField() {
  const role = document.getElementById("new-role")?.value;
  const field = document.getElementById("mentor-assign-field");
  if (field) field.style.display = role === "coach" ? "" : "none";
}

async function createUser() {
  const name = document.getElementById("new-name")?.value?.trim();
  const code = document.getElementById("new-code")?.value?.trim().toUpperCase();
  const role = document.getElementById("new-role")?.value;
  const gym = document.getElementById("new-gym")?.value;
  const mentorId = document.getElementById("new-mentor")?.value || "";

  if (!name || !code || !role) { showToast("Please fill in name, code, and role."); return; }

  try {
    // Check code uniqueness
    const existing = await db.collection("users").where("code","==",code).limit(1).get();
    if (!existing.empty) { showToast("That access code is already taken."); return; }

    const id = "user_" + Date.now();
    await db.doc(`users/${id}`).set({ name, code, role, gym, mentorId });
    showToast(`${name} created ✓`);
    await renderManageUsers(document.getElementById("main-content"));
  } catch(e) { showToast("Error: " + e.message); }
}

async function deleteUser(userId, name) {
  if (!confirm(`Remove ${name}? This cannot be undone.`)) return;
  try {
    await db.doc(`users/${userId}`).delete();
    showToast(`${name} removed`);
    await renderManageUsers(document.getElementById("main-content"));
  } catch(e) { showToast("Error: " + e.message); }
}

// ═══════════════════════════════════════════════════════════════════════
// HELPERS
// ═══════════════════════════════════════════════════════════════════════
function driveEmbedUrl(url) {
  if (!url) return "";
  // Google Drive file viewer
  const fileMatch = url.match(/\/file\/d\/([^/]+)/);
  if (fileMatch) return `https://drive.google.com/file/d/${fileMatch[1]}/preview`;
  // Already an embed
  if (url.includes("/preview") || url.includes("embed")) return url;
  return url;
}

function escHtml(str) {
  if (!str) return "";
  return String(str)
    .replace(/&/g,"&amp;")
    .replace(/</g,"&lt;")
    .replace(/>/g,"&gt;")
    .replace(/"/g,"&quot;")
    .replace(/'/g,"&#39;");
}

function showToast(msg) {
  const t = document.getElementById("toast");
  t.textContent = msg;
  t.classList.add("show");
  setTimeout(()=>t.classList.remove("show"), 2500);
}

// Close modal on overlay click
document.getElementById("session-modal").addEventListener("click", function(e) {
  if (e.target === this) closeModal();
});

// Start
init();
</script>
</body>
</html>
